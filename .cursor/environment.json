{
  "name": "Time to Heat",
  "ports": [
    {
      "name": "web server",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Static web server hosting the Time to Heat calculator (index.html). Open http://localhost:8000/ to use the app."
    }
  ]
}
